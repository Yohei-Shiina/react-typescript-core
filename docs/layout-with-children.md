## Added in this branch

- feature/layout-with-children: Added typed layout component

- Destruct props in the function parameters.
  - Improves readability by making used props explicit
  - Avoids React.FC because it implicitly adds children
